# Runbooks

Org-specific instructions for collecting each kind of evidence: where the
systems are, which accounts/roles to use, what to export, and what the
resulting artifact should look like. Agents follow these exactly — keep
them current, and improve them whenever a collection was painful.
